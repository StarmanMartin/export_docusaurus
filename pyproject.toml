[tool.poetry]
name = "export-docusaurus"
version = "0.1.0"
description = ""
authors = ["Martin <m.starman@live.comdd>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
